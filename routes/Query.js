// const Query = require("../model/Query");

// var route = require("express").Router();

// route.get("/", (req, res) => {});

// route.post("/create", async (req, res) => {
//   const { tags } = req.body;
//   const queryExist = await Query.find({tags});
//   if(queryExist){
//   }
// });

// module.exports = route;
